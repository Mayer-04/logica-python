"""
* Palabras reservadas en Python
--------------------------------
Las palabras reservadas son `términos especiales` en Python que tienen un significado específico.
- No se pueden usar para nombrar variables, funciones o clases. 
- Python las reserva para su propio uso interno (su funcionamiento).

Algunas de estas son:
---------------------
- `as:` Se usa para dar un nuevo nombre o alias a una variable.
- `assert:` Se utiliza para comprobar que una condición es verdadera. Si no lo es, el programa muestra un error.
- `async:` Se utiliza para definir una función asincróna.
- `await:` Se usa dentro de una función async para esperar a que otra tarea se termine.
- `is:` Compara dos objetos para ver si son el mismo objeto en la memoria.
- `def:` Se utiliza para crear una función nueva.
- `return:` Devuelve un valor desde una función al lugar donde se llamó.
- `del:` Elimina una variable o un objeto.
- `raise:` Lanza un error o excepción generado por el usuario.
- `pass:` Se utiliza para pasar un valor sin realizar ninguna operación. No hace nada.
- `None:` Representa la ausencia de valor, similar a null en otros lenguajes.
- `in:` Verifica si un elemento está dentro de una secuencia (como una lista o una tupla).
_ `global:` Indica que una variable dentro de una función se refiere a una variable global.
_ `lambda:` Crea una función pequeña y anónima.
- `nonlocal:` Se usa en funciones anidadas para referirse a una variable de un ámbito superior, pero que no es global.
- `with:` Maneja recursos, como archivos, y asegura que se liberen correctamente después de usarlos.
- `from:` Se usa junto con import para importar algo específico de un módulo.
- `import:` Se usa para incluir un módulo completo en tu programa.
- `yield:` Permite a una función devolver un valor y luego reanudar su ejecución, creando un generador.
- `except:` Se usa junto con `try` para manejar errores que pueden ocurrir en el bloque `try`.
- `finally:` Se usa para ejecutar un bloque de código después de un bloque try y except, 
sin importar si ocurrió un error o no.
- `for:` Se usa para crear un bucle que itera sobre una secuencia (como una lista o un rango).
- `while:` Se usa para crear un bucle que se repite mientras se cumpla una condición.
- `continue:` Hace que el programa pase a la siguiente iteración del bucle actual, 
omitiendo el resto del código en esa iteración.
- `break:` Se usa para salir de un bucle (como un for o while) antes de que se termine de ejecutar completamente.
- `if:` Se usa para ejecutar un bloque de código si se cumple una condición.
- `elif:` Se usa en estructuras condicionales para comprobar otra condición si la anterior no se cumplió.
- `else:` Se usa en estructuras condicionales para ejecutar un bloque de código si ninguna de las 
condiciones anteriores se cumplió.
- `or:` Se usa en expresiones condicionales para comprobar si al menos una de las condiciones es verdadera.
- `not:` Se usa para negar una condición, haciendo que una expresión sea verdadera si la condición original es falsa.
- `and:` Se usa en expresiones condicionales para combinar varias condiciones, la expresión completa solo es verdadera
si todas las condiciones individuales son verdaderas.
"""
